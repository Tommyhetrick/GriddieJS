// Griddie.js
// Tom Hetrick Jr.

function createGrid(tx,ty,options) {
  return new Griddie(tx,ty,options);
}

class Griddie {
  constructor(tx,ty,options) {
    this.tx = tx;
    this.ty = ty;
    this.options = options;
  }

}
